---

- hosts: all
  become: true
  tasks:

  - name: Install the bloody thing apache
    package:
      name:
        - "{{ apache_package }}"
        - "{{ php_package }}"
      state: latest
      update_cache: yes
