- name: install apache and php for Linux Mint servers
  tags: apache,apache2,mint 
  apt:
    name:
      - apache2
      - libapache2-mod-php
    state: latest
  when: ansible_distribution == "Linux Mint"

- name: start and enable apache Linux Mint servers
  tags: apache,apache2,mint
  service:
    name: apache2
    state: started
    enabled: yes
  when: ansible_distribution == "Linux Mint"

- name: copy default html file for site
  tags: apache,apache2,httpd,index.html
  copy:
    src: default_site.html
    dest: /var/www/html/index.html
    owner: root
    group: root
    mode: 0644

- name: install apache and php for Rocky Linux servers
  tags: apache,httpd,rocky
  dnf:
    name:
      - httpd
      - php
    state: latest
  when: ansible_distribution == "Rocky"

- name: start httpd (Rocky)
  tags: apache,rocky,httpd
  service:
    name: httpd
    state: started
    enabled: yes
  when: ansible_distribution == "Rocky" 

- name: change e-mail address for admin
  tags: apache,rocky,httpd
  lineinfile: 
    path: /etc/httpd/conf/httpd.conf
    regexp: '^ServerAdmin'
    line: ServerAdmin mikekaton21@gmail.com
  when: ansible_distribution == "Rocky"
  register: httpd

- name: restart httpd (Rocky)
  tags: apache,rocky,httpd
  service:
    name: httpd
    state: restarted
  when: httpd.changed
